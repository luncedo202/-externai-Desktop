# Azure App Service Deployment Configuration
# This file is used by Azure to build and deploy your app

runtime:
  name: node
  version: 18

build:
  commands:
    - npm install
    - npm run build || echo "No build script found"

run:
  command: node server.js

healthcheck:
  path: /health
  port: 8080
